Focus ONLY on **RPR Step 1**.

Do not work on Steps 2, 2A, 2B, 3, 4, scoring, portfolio aggregation, or any later workflow.

I want a working Step-1 implementation that I can test by copying the generated files into my existing backend/UI/prompts directories.

You already analysed the real Python backend. Use that previous analysis as the source of truth.

## STEP-1 FEATURE TO IMPLEMENT

Both Step-1 paths must continue working:

1. Trigger 1 — Market Scanner
2. Trigger 2 — User Narrative / R2D2

The new feature is:

**Run Step 1 → inspect result quality → if there is a genuine fixable weakness, recommend an enhancement → user accepts/edits/dismisses → accepted feedback is consolidated → user explicitly rescans → updated Step-1 result is shown.**

No automatic rescan.

No automatic permanent prompt modification.

---

# 1. FIRST: INSPECT ALL UPLOADED FILES

Before changing anything, compare:

* original backend Python files
* the Step-1 HTML
* the enhancement files you previously generated
* `prompt_loader.py`, if uploaded
* the three approved prompt files

Determine exactly what is already implemented and what is still missing.

DO NOT regenerate unrelated files.

DO NOT modify a file merely because it exists.

Only change files actually required for Step 1.

---

# 2. MARKET_EVENT_SCOUT.PY — TRIGGER 1

Use the real existing implementation:

`MarketEventScout.fetch_events()`

`MarketEventScout.refine_event()`

`_build_query()`

`MarketEventParser`

Keep the existing Trigger-1 architecture.

Add only what is necessary to support enhancement diagnosis.

The Step-1 Trigger-1 result must expose:

* theme
* real event ID
* event sections
* query generated by `_build_query()`
* query/queries actually executed
* retrieval success/failure
* evidence availability/quality per section where determinable

Do not change existing integer event IDs into UUIDs or strings.

Do not create `narrative_segment_id` for Trigger 1.

`MarketEventScout.refine_event()` is the existing Trigger-1 feedback/rescan hook. Reuse it rather than creating a second competing rescan architecture.

The enhancement feature must be able to pass the consolidated user refinement into this existing refinement path.

---

# 3. TRIGGER 2 — USE THE ACTUAL STEP-1 PATH

Your previous analysis found two possible implementations:

`narrative_enricher.enrich_narrative()`

and

`RPRService.enrich_narrative()`

For this task determine which one the uploaded Step-1 application actually uses.

Modify the ACTIVE Step-1 path.

Do not implement the enhancement twice unless both paths are genuinely called by Step 1.

The Trigger-2 result must expose enough information for diagnosis:

* original user narrative
* generated eight sections
* retrieval/search result
* retrieval status
* evidence quality where determinable

Keep the existing `R2D2_PROMPT` behaviour unless the uploaded code demonstrates a required integration change.

Do NOT replace the R2D2 production prompt with an enhancement prompt.

---

# 4. RPR_ENHANCEMENT.PY

If the previously generated `rpr_enhancement.py` is useful, adapt it to the REAL backend.

Do not leave it as a parallel theoretical architecture.

It should contain the common Step-1 enhancement logic such as:

* diagnosis request/response models
* recommendation status
* evidence/retrieval status models
* feedback consolidation
* duplicate recommendation protection
* construction of runtime refinement instruction

Its IDs and scope types MUST match the real backend.

For Trigger 1 use the real theme/event identifiers.

For Trigger 2 use the identifiers that actually exist.

Remove assumptions such as `narrative_segment_id` if no such concept exists.

---

# 5. THE THREE APPROVED PROMPTS

The three approved prompts are:

1. `trigger1_market_scanner`
2. `enhancement_diagnosis`
3. `feedback_consolidation`

Do NOT rewrite their approved content.

Inspect `prompt_loader.py`.

Determine exactly:

* how prompt files are located
* required extension
* expected directory
* exact names passed to `get_prompt()`

Then make the implementation consistent with the existing PromptLoader.

If PromptLoader expects:

`get_prompt("enhancement_diagnosis")`

then ensure the corresponding prompt file exists with the exact filename/location expected by PromptLoader.

Do the same for `feedback_consolidation` and `trigger1_market_scanner` where it is genuinely called.

Do NOT guess prompt paths.

Do NOT modify `prompt_loader.py` unless the existing loader cannot load these prompts and a minimal backward-compatible change is actually required.

---

# 6. IMPORTANT — LLM_GATEWAY.PY

Your previous backend analysis identified `llm_gateway.py` as an EXISTING AI integration point.

That does NOT mean it needs modification.

Do NOT modify `llm_gateway.py` unless the Step-1 implementation genuinely cannot work through its existing:

`R2D2LLMGateway.call()`

or

`R2D2LLMGateway.call_raw()`

interfaces.

Prefer using the existing gateway exactly as it is.

If no change is required, leave `llm_gateway.py` untouched.

---

# 7. EVIDENCE / RETRIEVAL STATUS

Implement the distinction needed by the approved diagnosis prompt:

* SUFFICIENT
* LIMITED
* NONE
* NOT_ASSESSED

and retrieval status such as:

* SUCCESS
* TECHNICAL_FAILURE
* PARTIAL_TECHNICAL_FAILURE
* NOT_ASSESSED

Use the exact approved prompt terminology where specified.

Critical rule:

A technical search failure is NOT the same thing as “no evidence found.”

Do not fabricate evidence status.

If retrieval failed, preserve that fact.

---

# 8. FIX THE `None` WEB-CONTENT ISSUE IF PRESENT IN THE ACTIVE STEP-1 PATH

Your previous analysis found that one Trigger-2 path could pass:

`web_content = None`

into Claude as though `"None"` were retrieved evidence.

If that issue still exists in the ACTIVE Step-1 path, fix it.

Claude must not receive literal `"None"` represented as valid search evidence.

Instead explicitly indicate retrieval failure/unavailability.

Do not perform unrelated refactoring.

---

# 9. DIAGNOSIS

After the normal Trigger-1 or Trigger-2 Step-1 result exists, call the approved:

`enhancement_diagnosis`

prompt.

The diagnosis must receive:

* report content
* section information
* executed-query information when available
* evidence tier
* retrieval status

If result is:

`NOT_RECOMMENDED`

then retain the normal Step-1 screen.

Do not show an unnecessary enhancement recommendation.

If result is:

`RECOMMENDED`

return:

* diagnosis/reason
* suggested feedback
* affected section/scope if available

to the Step-1 frontend.

---

# 10. STEP-1 HTML

Use the uploaded current Step-1 HTML.

Do NOT redesign the page.

The existing feedback area at the bottom is where the feature should integrate.

For RECOMMENDED show a compact card:

**Potential improvement detected**

Reason:
[diagnosis]

Suggested refinement:
[editable suggestion]

Actions:

`Apply & Rescan`

`Edit`

`Dismiss`

Behaviour:

### Dismiss

Close the recommendation.
Do nothing else.

### Edit

Allow the user to edit the suggestion.

### Apply & Rescan

Only after this explicit user action:

1. call `feedback_consolidation`
2. create the final refinement instruction
3. invoke the appropriate existing rescan mechanism
4. display the returned result in the existing eight-section Step-1 UI

Do NOT automatically rescan when diagnosis completes.

---

# 11. FEEDBACK CONSOLIDATION

Use the approved:

`feedback_consolidation`

prompt.

Inputs must include the relevant:

* original user intent
* original narrative/theme/event
* diagnosis
* suggested refinement
* user's accepted/edited text

Its output becomes runtime refinement guidance.

It must NOT permanently modify:

* `R2D2_PROMPT`
* Trigger-1 master prompt
* any other production prompt

---

# 12. EXISTING NORMAL FEEDBACK MUST STILL WORK

The existing feedback box/icon already shown at the bottom of Step 1 must not be broken.

The enhancement recommendation is an additional guided capability.

Normal manual user feedback must continue working.

---

# 13. DUPLICATE RECOMMENDATIONS

During the same Step-1 browser/session run, do not repeatedly show exactly the same recommendation for the same Trigger-1 event or Trigger-2 narrative after the user has dismissed/applied it.

Use lightweight state.

No database.

---

# 14. STATIC/DEMO MODE

The current HTML can run with static/demo data.

Do not destroy this capability.

Where possible preserve the existing static fallback while adding backend integration.

The objective is that I can first test Step 1 without affecting the rest of the RPR application.

---

# 15. DO NOT TOUCH THESE UNLESS STRICTLY NECESSARY

Do not modify:

* Step 2 code
* Step 2A
* Step 2B
* Step 3
* Step 4
* scoring service
* portfolio aggregation
* comment publishing
* unrelated backend modules
* `llm_gateway.py` unless technically required
* production prompts unrelated to Step 1

---

# 16. OUTPUT FILES

I need files I can directly copy into my project.

For EVERY modified file:

Generate the COMPLETE file, not a patch and not a code fragment.

For EVERY new file:

Generate the COMPLETE file.

Preserve the original directory structure.

Expected categories may include, ONLY where actually needed:

`backend/market_event_scout.py`

active Trigger-2 Python file

`backend/rpr_enhancement.py`

Step-1 HTML

required prompt files under the real prompts directory

and any actual Step-1 route file if it was uploaded and genuinely requires modification.

Do not fabricate a route file if one was not supplied.

---

# 17. PROMPT FILES

Include the approved prompt files in the final package using the exact directory/name required by `PromptLoader`.

Explicitly state in the final summary:

`PromptLoader lookup -> actual prompt filename`

for all three prompts.

Example format:

`enhancement_diagnosis -> prompts/enhancement_diagnosis.txt`

But use the REAL discovered mapping, not this example.

---

# 18. VALIDATE STEP 1 ONLY

Before delivering, verify:

Trigger 1 normal scan works.

Trigger 2 normal enrichment works.

Eight sections still render.

Diagnosis can return NOT_RECOMMENDED.

Diagnosis can return RECOMMENDED.

Recommendation appears in the existing feedback area.

Dismiss does not rescan.

Edit permits user changes.

Apply & Rescan invokes feedback consolidation.

Trigger 1 refinement uses the existing Trigger-1 rescan hook.

Trigger 2 reruns its active enrichment path.

New result replaces/updates the existing Step-1 result.

Normal manual feedback still works.

No master prompt is permanently rewritten.

No Step 2–4 behaviour is required for this test.

Python syntax checks pass.

Frontend JavaScript has no obvious syntax errors.

Imports resolve against the uploaded files.

---

# 19. FINAL DELIVERY

Do not give me another architecture report.

Return:

1. `STEP1_IMPLEMENTATION_STATUS.md`
2. every complete modified/new file individually downloadable
3. the three prompt files in their correct real locations
4. one ZIP named:

`rpr_step1_enhancement_test.zip`

The ZIP must preserve the exact folder structure so I can copy the backend files into the backend folder, UI file into the UI folder, prompt files into the prompts folder, and test Step 1.

Finally provide a SHORT table:

| File | Existing/New | Changed? | Destination | Purpose |

Also explicitly state:

**Files that were inspected but intentionally NOT changed.**

Do not claim `llm_gateway.py` was changed if it was not.

Start by inspecting the files, then implement and generate the actual files.
